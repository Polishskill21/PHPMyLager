# Application Settings
APP_PORT=8000
APP_KEY=

# Database Settings
DB_DATABASE=warehouse_storage
DB_USERNAME=laravel
DB_PASSWORD=
DB_ROOT_PASSWORD=

# Seeding Passwords
# Per-role seed account passwords. If a role-specific value is empty the seeder
# falls back to SEEDER_PASSWORD, then to the literal 'password'.
SEEDER_PASSWORD=
SEEDER_ADMIN_PASSWORD=
SEEDER_WRITER_PASSWORD=
SEEDER_VIEWER_PASSWORD=